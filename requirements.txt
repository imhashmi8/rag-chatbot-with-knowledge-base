cat > requirements.txt <<'EOF'
python-dotenv>=1.0.0
gradio>=4.44.0
pandas>=2.0.0
pydantic>=2.0.0
tenacity>=8.0.0
tqdm>=4.66.0

# Baseline RAG (implementation/)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0

# Vector store + embeddings backbone
chromadb>=0.5.0
openai>=1.40.0

# Pro RAG (pro_implementation/) — reranking, query rewriting via multiple providers
litellm>=1.50.0
EOF

